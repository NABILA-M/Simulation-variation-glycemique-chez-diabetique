
cat("***begin .Rprofile Simulation glycemie***\n\n")

#Chargement des packages

if (require(tidyverse)==FALSE) {
	utils::install.packages("tidyverse")
}
library(tidyverse)

if (require(lubridate)==FALSE) {
	utils::install.packages("lubridate")
}
library(lubridate)

if (require(plotly)==FALSE) {
	utils::install.packages("plotly")
}
library(plotly)

cat ("***End.Rprofile Simulation glycemie***\n\n")

